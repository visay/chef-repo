= DESCRIPTION:

This cookbook provides recipes for setting up default useful package on system.

= REQUIREMENTS:

Depends on cookbook: php, jenkins,

= ATTRIBUTES:

None

= USAGE:

"recipe[base]"  -> set up default package (e.g curl, wget , ...)
"recipe[base::firewall]" -> set up default firewall rules

