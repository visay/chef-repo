cookbooks:

  - base:                # -> custom recipe
#  - chef-client:
  - apt:
  - ntp:
  - php-ci:               # -> custom recipe
  
  - build-essential:
  - xml:                 # -> dependency mysql
  - openssl:             # -> dependency mysql
  - runit:               # -> dependency jenkins
  - java:                # -> dependency jenkins

  - apache2:
  - mysql:
  - php:
  - typo3:               # -> custom recipe
  - jenkins:

roles:
  - base:
  - web:
  - web-typo3:
  - ci:
     
#data bags:
#  - apps:
#    - typo3

#nodes:
#  - mango:
#    - role[base], role[web]
#    - -x fudriot --sudo 


# knife node run_list add mango 'role[base]'
# knife ssh name:mango -x fudriot "sudo chef-client"