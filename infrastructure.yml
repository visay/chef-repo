cookbooks:
  - base:                # (site-cookbook)
  - apt:
  - ntp:
  - iptables:
  - user:
  - yum:
  - git:
  - zsh:
  - oh-my-zsh:
  - build-essential:
  - xml:                 # -> dependency mysql
  - openssl:             # -> dependency mysql
  - runit:               # -> dependency jenkins
  - java:                # -> dependency jenkins
  - jpackage:
  - ant:

  ############
  - apache2:
  - mysql:
  - php:
  - typo3:               # (site-cookbook)
  - jenkins:
  - testing:             # (site-cookbook)

roles:
  - base:
  - typo3-server:
  - ci-server:

data bags:
  - users:
    - essentials

#nodes:
#  - ci.COMPANY.COM:
#    - role[ci-server]
#    - -x fudriot --sudo
